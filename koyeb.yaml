name: helix-discord-bot
services:
  - name: bot
    type: web
    buildpack:
      build_command: npm ci --include=dev && npm --prefix HELIX ci --include=dev && npm run build
      run_command: npm start
    instance_type: free
    regions:
      - fra
    health_checks:
      - path: /api/health
        port: 5000
    env:
      - key: DISCORD_TOKEN
        value: ""
      - key: DISCORD_CLIENT_ID
        value: ""
      - key: DISCORD_CLIENT_SECRET
        value: ""
      - key: NEXTAUTH_SECRET
        value: ""
      - key: NODE_ENV
        value: "production"
      - key: PORT
        value: "5000"
    ports:
      - port: 5000
        path: /
        protocol: http
