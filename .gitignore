# Node
node_modules/
dist/
build/
*.tsbuildinfo
*.log

# Runtime data (SQLite database lives here)
data/

# Secrets (self-hosting env file; never commit)
.env

# Cookie jars / captured challenge state (local testing only)
data-cookies.txt
*.cookies.txt

# Local scratch / temporary work
scratch/

# Coverage
coverage/
.nyc_output/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Lock files for alternative package managers (npm lock file is committed)
npm-shrinkwrap.json
.pnpm-lock.yaml
yarn.lock

# Keep plan files under source control, but don't commit them.
.agents/plans/**

# Whitelist files in untracked locations to be tracked
# This section explicitly whitelists the files that should be tracked.
!./agents/plans/roadmap.md
!./agents/plans/phase*.md

# Temporary files that are created during development and should not be tracked.
IMPORTANT.md