{
  "name": "helix",
  "version": "1.0.0",
  "description": "HELIX — the autonomous Discord bot with an embedded AI assistant, scaffolding engine, and companion dashboard",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "build": "tsc",
    "start": "node src/dist/index.js",
    "dev": "tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "picocolors": "^1.1.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  }
}
